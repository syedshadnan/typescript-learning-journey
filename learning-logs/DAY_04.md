# Day 04

## Concepts Learned

* Functions in TypeScript
* Parameter type annotations
* Return type annotations
* Return type inference
* Optional parameters
* `undefined` with optional parameters
* Default parameters
* Difference between optional and default parameters
* Function contracts
* Compile-time/type-checking errors vs runtime behavior
* TypeScript function typing in real-world scenarios

## Practice Completed

* Created a typed `calculateArea()` function
* Created a `greetUser()` function with an optional parameter
* Created a `calculateDiscount()` function with a default parameter
* Created a typed `createProfile()` function using multiple parameter types
* Practiced arrow functions with TypeScript
* Practiced handling `undefined` with optional parameters
* Completed concept verification, mini quiz, and interview questions

## Common Mistakes

* Initially confused a TypeScript type-checking error with a runtime error
* Needed clarification that `parameter?: type` conceptually means `type | undefined`
* Initially described return type inference as being based only on parameter types
* Needed to distinguish a missing parameter value (`undefined`) from a function returning `undefined`

## Interview Questions Missed

* Precise distinction between TypeScript compile-time/type-checking errors and JavaScript runtime behavior
* Why TypeScript needs parameter type information to safely infer types in some situations

## Strengths

* Strong understanding of parameter type annotations
* Strong understanding of explicit return types
* Good grasp of return type inference
* Correctly handles optional parameters
* Understands default parameters and fallback values
* Good practical reasoning about function design
* Able to think about function signatures as contracts
* Successfully applied TypeScript concepts without relying on basic JavaScript explanations

## Weak Areas

* Continue practicing compile-time vs runtime behavior
* Reinforce `undefined` handling with optional parameters
* Improve precision when explaining how TypeScript performs type inference

## Interview Readiness

**9.3/10**

Demonstrated strong understanding of TypeScript functions, parameters, return types, optional parameters, and default parameters.

## Confidence Score

**9/10**

## Next Focus

* Union types
* Combining multiple possible types
* Type-safe handling of values with multiple possible types
* Practical and real-world examples of union types
